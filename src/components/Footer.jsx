export default function Footer() {
  return (
    <footer className="w-full mt-auto bg-card text-card-foreground border-t px-6 py-4 text-sm text-center">
      <p>
        © {new Date().getFullYear()} Delhi Technological University — Training & Placement Cell
      </p>
    </footer>
  );
}
